export const dataFunciontReturn = {
  dataEsJS:
    "En JavaScript, las funciones pueden devolver valores usando la palabra clave 'return'. Esto permite que el resultado de la función sea utilizado por otras partes del programa.",
  dataEnJS:
    "In JavaScript, functions can return values using the 'return' keyword. This allows the result of the function to be used by other parts of the program.",
  codigoJS: `
      function suma(a, b) {
        return a + b; // Devuelve la suma de a y b
      }
  
      let resultado = suma(2, 3);
      console.log(resultado); // Imprime 5
    `,
  dataEsPY:
    "En Python, las funciones pueden devolver valores utilizando la palabra clave 'return'. Esto permite que el resultado de la función sea utilizado por otras partes del programa.",
  dataEnPY:
    "In Python, functions can return values using the 'return' keyword. This allows the result of the function to be used by other parts of the program.",
  codigoPY: `
      def suma(a, b):
          return a + b  # Devuelve la suma de a y b
  
      resultado = suma(2, 3)
      print(resultado)  # Imprime 5
    `,
  dataEsJAVA:
    "En Java, las funciones (o métodos) pueden devolver valores utilizando la palabra clave 'return'. Esto permite que el resultado del método sea utilizado por otras partes del programa.",
  dataEnJAVA:
    "In Java, functions (or methods) can return values using the 'return' keyword. This allows the result of the method to be used by other parts of the program.",
  codigoJAVA: `
      public class Main {
          public static int suma(int a, int b) {
              return a + b; // Devuelve la suma de a y b
          }
  
          public static void main(String[] args) {
              int resultado = suma(2, 3);
              System.out.println(resultado); // Imprime 5
          }
      }
    `,
  dataEsExplication:
    "Las funciones permiten que un programa organice y reutilice código de manera eficiente. En esta sección, veremos ejemplos de funciones con retorno de valores.",
  dataEnExplication:
    "Functions allow a program to organize and reuse code efficiently. In this section, we will look at examples of functions with return values.",
};
